{
  "name": "lcampassi-portfolio",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Lucciano Campassi — Cybersecurity Portfolio",
  "scripts": {
    "dev": "vite --port 8080",
    "build": "vite build",
    "preview": "vite preview --port 8080",
    "serve": "vite preview --port 8080 --host"
  },
  "devDependencies": {
    "vite": "^6.3.0"
  }
}
